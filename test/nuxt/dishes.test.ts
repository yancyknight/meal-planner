// API route tests live in test/unit/dishes-api.test.ts (requires a running Nitro server,
// which is not available in the nuxt vitest environment).
// This file is intentionally empty for now.
import { describe, it } from 'vitest'

describe('dishes nuxt tests (placeholder)', () => {
  it.todo('component tests will be added when dish components have non-trivial internal logic')
})
