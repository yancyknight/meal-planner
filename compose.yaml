services:
  app:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - app_data:/data
    environment:
      DATABASE_URL: /data/app.db
      IMAGE_DIR: /data/images
      NUXT_PUBLIC_APP_NAME: Meal Planner
    restart: unless-stopped

volumes:
  app_data:
